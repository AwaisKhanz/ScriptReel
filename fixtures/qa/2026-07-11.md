# QA & benchmarks — 2026-07-11 (Phase 14, M3 Pro 19 GB)

## Matching evaluation (`pnpm eval:matching`)

- **precision@1 (top-1 acceptance) = 100%** · gate ≥ 70% at Phase 14 → **PASS**
- 30 labeled pairs (16 good / 14 bad), 6 beats from G1–G6
- operating points recovered: τ_hi = 0.322 (@90% precision), τ_lo = 0.314 (@70%) — matches `constants.ts`

## Render quality

- **G1** (45 s city-morning, 6 beats), Draft 720p, 16:9 → 41 s render, 15.6 MB
- beat kinds: `video ×6` — **0% text-card fallback** · gate < 20% on G1–G5 → **PASS**

## Timing (measured this session, M3 Pro)

| Scenario | Target | Actual |
|---|---|---|
| G1 (45 s, 6 beats) Draft, **cold** cache, E2E | — | **150 s** |
| `composeOnly` after music change (bees, 2 beats) | ≤ 90 s | ~8 s |
| Single beat re-search + re-score | ≤ 8 s | ~5 s (3 provider requests) |
| Approve → align+fetch+compose (bees, 2 beats) | — | ~22 s |

**Disk-gated (not run):** the 3-minute warm/cold Draft & Final benchmarks and peak-RSS
profiling need the full G1–G8 golden run, which requires more free disk than was available
(1.7 GB at the time; the run needs ~2 GB+). The disk guard (`E_DISK_FULL`) and LRU cache
eviction added in this phase exist to keep those runs from failing ugly. Re-run with ≥ 15 GB
free to capture the 3-minute numbers.

## Notes

- Cold cache adds model warmup + downloads + SigLIP embedding on top of the warm path;
  the 45 s G1 cold E2E of 150 s implies a warm 45 s render well under a minute.
- The pipeline never hard-failed across ~15 renders this session; connection/sidecar blips
  now self-heal via pg-boss retry (see `handler.ts`, `sidecar/client.ts`).
