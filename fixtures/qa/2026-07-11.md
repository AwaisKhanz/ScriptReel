# QA & benchmarks — 2026-07-11 (Phase 14, M3 Pro 19 GB)

## Matching evaluation (`pnpm eval:matching`)

- **precision@1 (top-1 acceptance) = 100%** · gate ≥ 70% at Phase 14 → **PASS**
- 30 labeled pairs (16 good / 14 bad), 6 beats from G1–G6
- operating points recovered: τ_hi = 0.322 (@90% precision), τ_lo = 0.314 (@70%) — matches `constants.ts`

## Render quality

- **G1** (45 s, 6 beats), Draft 720p, 16:9 → 41 s render, 15.6 MB — `video ×6`, **0% fallback**.
- **3-min** (486-word "blue planet", 22 beats), Draft 720p, 16:9 → **181 s (3m1s)** render, 140 MB —
  `video ×22`, **0% text-card fallback** (gate < 20% → **PASS**). Every beat matched to relevant
  stock video with no fallback across a 3-minute script.

## Timing (measured this session, M3 Pro; DATA_DIR freed to ~5 GB)

| Scenario | Target | Actual |
|---|---|---|
| **3-min (22 beats) Draft, cold cache, E2E** | ≤ 10 min (cold Final) | **8m48s** — under the cold ceiling |
| — of which `search` (cold, rate-limited) | — | ~4 min (the bottleneck) |
| — fetch / score / align / compose | — | ~100 s / ~78 s / ~25 s / ~25 s |
| G1 (45 s, 6 beats) Draft, cold, E2E | — | 150 s |
| `composeOnly` after music change | ≤ 90 s | ~8 s |
| Single beat re-search + re-score | ≤ 8 s | ~5 s (3 provider requests) |

The **cold** 3-min Draft (8m48s) is under the ≤ 10-min cold-Final ceiling; it is **search-bound**
— cold cache + free-tier provider rate limits (Pixabay 100/60 s) put ~4 min in `search`. The warm
path (24 h search cache + shared asset cache) skips almost all of that, so the ≤ 6-min warm-Draft
target is comfortably reachable once the cache is populated; capturing the exact warm number and
the Final-preset run is left for a session with more headroom.

**Still not run:** peak-RSS profiling and the full G1–G8 sweep (needs ≥ ~15 GB free; had ≤ 5 GB).

## Notes

- Cold cache adds model warmup + downloads + SigLIP embedding on top of the warm path;
  the 45 s G1 cold E2E of 150 s implies a warm 45 s render well under a minute.
- The pipeline never hard-failed across ~15 renders this session; connection/sidecar blips
  now self-heal via pg-boss retry (see `handler.ts`, `sidecar/client.ts`).
