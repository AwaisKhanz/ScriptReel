{
  "name": "@scriptreel/voice",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn server:app --host 127.0.0.1 --port 8585",
    "start": "uv run uvicorn server:app --host 127.0.0.1 --port 8585"
  }
}
